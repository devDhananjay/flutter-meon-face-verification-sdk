name: meon_face_verification
description: Flutter SDK for Meon Face Verification, mirroring the existing React Native component.
version: 0.1.2
homepage: https://face-finder.meon.co.in

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_inappwebview: ^6.0.0
  permission_handler: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

